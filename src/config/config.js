const host = "http://localhost:8080";
const Config = {
  HOST: host,
  LOGIN_ENDPOIN: host + "/api/auth/signin",
  SIGNUP_ENDPOINT: host + "/api/auth/signup",
  PROFILE_ENDPOINT: host + "/api/profile",
};

export default Config;
